function [codebook] = fillCodeBook(n, vectors)
    codebook = zeros(n, size(vectors,2));
    for i = 1:n
        codebook(:,i) = vectors(:,randi([(i-1) * size(vectors,2) / n + 1, i * size(vectors,2) / n + 1],1,1));
    end
end